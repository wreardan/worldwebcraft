#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../browservefy/bin/browservefy" "$@"
  ret=$?
else 
  node  "$basedir/../browservefy/bin/browservefy" "$@"
  ret=$?
fi
exit $ret
